import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { toast } from "sonner";
import { Activity, ArrowUpRight, Bell, ChevronDown, CircleHelp, Copy, CreditCard, Download, KeyRound, LayoutDashboard, LifeBuoy, LogOut, Menu, MoreHorizontal, Plus, Search, Settings2, Smartphone, Users, X } from "lucide-react";
import { useState } from "react";

function Logo() { return <div className="flex items-center gap-2.5 text-[#f9f4ed]"><span className="grid h-8 w-8 place-items-center rounded-[10px] bg-[#e11d2e] text-sm font-black text-[#171717]">s.</span><span className="text-[17px] font-semibold tracking-[-.04em]">snyx.store.api</span></div>; }
function StatusPill({ status }: { status: string }) { const labels: Record<string, string> = { active: "Ativa", trial: "Em teste", paused: "Pausada", expired: "Expirada", revoked: "Revogada" }; const styles: Record<string, string> = { active: "bg-[#d9f5b2] text-[#263b15]", trial: "bg-[#f4d06f] text-[#5b4500]", paused: "bg-[#d7e7fa] text-[#284a69]", expired: "bg-black/10 text-black/60", revoked: "bg-[#ffd5d9] text-[#8d1120]" }; return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[10px] font-bold ${styles[status] || "bg-black/10 text-black/60"}`}><span className="h-1.5 w-1.5 rounded-full bg-current" />{labels[status] || status}</span>; }
function formatTime(value: Date | string | null) { if (!value) return "sem data"; return new Date(value).toLocaleString("pt-BR", { day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit" }); }

export default function AdminDashboard() {
  const [navOpen, setNavOpen] = useState(false); const [active, setActive] = useState("Visão geral"); const [search, setSearch] = useState("");
  const { user, loading, logout } = useAuth();
  const summaryQuery = trpc.dashboard.summary.useQuery(undefined, { enabled: Boolean(user?.role === "admin"), retry: false });
  const summary = summaryQuery.data;
  const filtered = (summary?.activity || []).filter((item) => `${item.name} ${item.email} ${item.key}`.toLowerCase().includes(search.toLowerCase()));
  if (loading || (user?.role === "admin" && summaryQuery.isLoading)) return <div className="grid min-h-screen place-items-center bg-[#171717] text-[#f9f4ed]"><div className="h-8 w-8 animate-spin rounded-full border-2 border-[#e11d2e] border-t-transparent" /></div>;
  if (!user) return <div className="grid min-h-screen place-items-center bg-[#171717] px-5 text-[#f9f4ed]"><div className="max-w-md text-center"><div className="mx-auto mb-7 grid h-16 w-16 place-items-center rounded-3xl bg-[#e11d2e] text-2xl font-black text-[#171717] shadow-[5px_5px_0_#d9f5b2]">s.</div><h1 className="text-4xl font-semibold tracking-[-.07em]">Seu console começa aqui.</h1><p className="mt-4 text-sm leading-6 text-[#f9f4ed]/50">Entre para gerenciar clientes, planos, licenças e dispositivos do snyx.store.api.</p><Button onClick={() => startLogin()} className="mt-8 h-12 rounded-full bg-[#e11d2e] px-7 font-semibold text-[#171717] hover:bg-[#f43f4f]">Entrar no painel <ArrowUpRight className="ml-2 h-4 w-4" /></Button></div></div>;
  if (user.role !== "admin") return <div className="grid min-h-screen place-items-center bg-[#f6f1e9] px-5"><div className="max-w-md text-center"><h1 className="text-3xl font-semibold tracking-[-.06em]">Acesso restrito</h1><p className="mt-3 text-sm text-black/55">Esta área é exclusiva para administradores do snyx.store.api.</p><Button onClick={() => window.location.href = "/"} className="mt-7 rounded-full bg-[#171717] text-[#f9f4ed]">Voltar para a vitrine</Button></div></div>;
  const totals = summary?.totals || [];
  return <div className="min-h-screen bg-[#f6f1e9] text-[#171717] lg:flex"><aside className={`${navOpen ? "translate-x-0" : "-translate-x-full"} fixed inset-y-0 left-0 z-50 w-[270px] bg-[#171717] p-5 text-[#f9f4ed] transition-transform lg:relative lg:translate-x-0`}><div className="flex items-center justify-between"><Logo /><button onClick={() => setNavOpen(false)} className="lg:hidden"><X className="h-5 w-5" /></button></div><div className="mt-14 text-[10px] font-bold uppercase tracking-[.2em] text-[#f9f4ed]/30">WORKSPACE</div><nav className="mt-4 space-y-1">{[[LayoutDashboard,"Visão geral"],[KeyRound,"Licenças"],[Users,"Clientes"],[CreditCard,"Assinaturas"],[Activity,"Atividade"]].map(([Icon, label]) => <button key={label as string} onClick={() => { setActive(label as string); setNavOpen(false); }} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm transition ${active === label ? "bg-[#e11d2e] font-semibold text-[#171717]" : "text-[#f9f4ed]/55 hover:bg-white/5 hover:text-[#f9f4ed]"}`}><Icon className="h-4 w-4" />{label as string}{label === "Licenças" && <span className="ml-auto rounded-full bg-white/10 px-2 py-0.5 text-[10px]">{summary?.totals?.[1]?.value || "0"}</span>}</button>)}</nav><div className="mt-12 text-[10px] font-bold uppercase tracking-[.2em] text-[#f9f4ed]/30">CONFIGURAÇÕES</div><nav className="mt-4 space-y-1">{[[Settings2,"Preferências"],[LifeBuoy,"Suporte"]].map(([Icon, label]) => <button key={label as string} onClick={() => toast.info(`${label} estará disponível em breve.`)} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm text-[#f9f4ed]/55 transition hover:bg-white/5 hover:text-[#f9f4ed]"><Icon className="h-4 w-4" />{label as string}</button>)}</nav><div className="absolute inset-x-5 bottom-5 border-t border-white/10 pt-5"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#d9f5b2] text-xs font-bold text-[#171717]">{(user.name || "S").slice(0, 1).toUpperCase()}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold">{user.name || "Admin"}</p><p className="truncate text-[10px] text-white/40">{user.email || "sem e-mail"}</p></div><button onClick={() => logout()} className="text-white/35 hover:text-white" title="Sair"><LogOut className="h-4 w-4" /></button></div></div></aside>{navOpen && <div className="fixed inset-0 z-40 bg-black/40 lg:hidden" onClick={() => setNavOpen(false)} />}
    <main className="min-w-0 flex-1"><header className="flex h-[76px] items-center justify-between border-b border-[#171717]/10 bg-[#f6f1e9] px-5 lg:px-10"><div className="flex items-center gap-4"><button onClick={() => setNavOpen(true)} className="lg:hidden"><Menu className="h-5 w-5" /></button><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#e11d2e]">CONSOLE / {active.toUpperCase()}</p><h1 className="mt-1 text-xl font-semibold tracking-[-.04em]">Bom dia, {user.name?.split(" ")[0] || "admin"}.</h1></div></div><div className="flex items-center gap-3"><button className="relative grid h-10 w-10 place-items-center rounded-full border border-[#171717]/10 hover:bg-white"><Bell className="h-4 w-4" /></button><div className="hidden h-8 w-px bg-[#171717]/10 sm:block" /><div className="hidden items-center gap-2 text-xs font-medium sm:flex"><div className="h-7 w-7 rounded-full bg-[#e11d2e]" />{user.name || "Admin"}<ChevronDown className="h-3.5 w-3.5 opacity-50" /></div></div></header>
      <div className="p-5 lg:p-10"><div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm text-[#171717]/45">Dados reais do banco de licenças</p><div className="mt-2 flex items-center gap-3"><span className="rounded-full bg-[#d9f5b2] px-2.5 py-1 text-[10px] font-bold text-[#263b15]">● SISTEMA SAUDÁVEL</span><span className="text-xs text-[#171717]/35">Atualizado agora</span></div></div><div className="flex gap-2"><Button variant="outline" onClick={() => toast.info("Exportação será habilitada após existirem registros reais.")} className="h-10 rounded-full border-[#171717]/15 bg-transparent text-xs"><Download className="mr-2 h-3.5 w-3.5" /> Exportar</Button><Button onClick={() => toast.info("O cadastro de licenças será habilitado no próximo passo.")} className="h-10 rounded-full bg-[#171717] text-xs text-[#f9f4ed] hover:bg-[#333]"><Plus className="mr-2 h-3.5 w-3.5" /> Nova licença</Button></div></div>
        <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{totals.map((item) => <div key={item.label} className={`rounded-[20px] p-5 ${item.tone === "coral" ? "bg-[#e11d2e]" : item.tone === "lime" ? "bg-[#d9f5b2]" : item.tone === "amber" ? "bg-[#f4d06f]" : "bg-[#d7e7fa]"}`}><div className="flex items-start justify-between"><span className="text-xs font-semibold opacity-60">{item.label}</span>{item.label === "MRR" ? <CreditCard className="h-4 w-4 opacity-55" /> : item.label.includes("Licenças") ? <KeyRound className="h-4 w-4 opacity-55" /> : item.label === "Em teste" ? <Activity className="h-4 w-4 opacity-55" /> : <Smartphone className="h-4 w-4 opacity-55" />}</div><div className="mt-7 flex items-end justify-between"><strong className="text-3xl font-semibold tracking-[-.07em]">{item.value}</strong><span className="text-[10px] font-bold opacity-60">{item.change}</span></div></div>)}</div>
        <div className="mt-4 grid gap-4 xl:grid-cols-[1.4fr_.6fr]"><section className="rounded-[22px] bg-[#171717] p-6 text-[#f9f4ed]"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#e11d2e]">RECEITA RECORRENTE</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.06em]">{summary?.totals?.[0]?.change === "Sem assinaturas" ? "Ainda não há receita." : "Receita conectada."}</h2></div></div><div className="grid h-[190px] place-items-center border-b border-white/10 text-center"><div><CreditCard className="mx-auto h-8 w-8 text-white/20" /><p className="mt-3 text-sm text-white/45">{summary?.totals?.[0]?.change || "Sem dados de pagamentos"}</p><p className="mt-1 text-[10px] text-white/25">Os valores aparecem quando o Stripe receber pagamentos reais.</p></div></div></section><section className="rounded-[22px] bg-[#d9f5b2] p-6"><div className="flex items-start justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#171717]/45">DISTRIBUIÇÃO REAL</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.06em]">Licenças</h2></div></div><div className="mt-10 flex items-center justify-center"><div className="relative grid h-40 w-40 place-items-center rounded-full" style={{ background: `conic-gradient(#171717 0 ${summary?.distribution.active ? 100 : 0}%, #f4d06f ${summary?.distribution.active ? 100 : 0}% 100%)` }}><div className="grid h-28 w-28 place-items-center rounded-full bg-[#d9f5b2]"><div className="text-center"><strong className="block text-3xl tracking-[-.08em]">{summary?.distribution.active || 0}</strong><span className="text-[10px] opacity-50">ativas</span></div></div></div></div><div className="mt-7 flex justify-between text-[10px] font-semibold"><span><i className="mr-1 inline-block h-2 w-2 rounded-full bg-[#171717]" />Ativas {summary?.distribution.active || 0}</span><span><i className="mr-1 inline-block h-2 w-2 rounded-full bg-[#f4d06f]" />Teste {summary?.distribution.trial || 0}</span></div></section></div>
        <section className="mt-4 rounded-[22px] border border-[#171717]/10 bg-[#fffdf8] p-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#e11d2e]">ATIVIDADE REAL</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.06em]">Licenças cadastradas.</h2></div><div className="flex items-center gap-2 rounded-full border border-[#171717]/10 px-3 py-2"><Search className="h-3.5 w-3.5 opacity-40" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar cliente ou chave" className="w-full bg-transparent text-xs outline-none placeholder:text-[#171717]/35 md:w-48" /></div></div><div className="mt-6 overflow-x-auto">{filtered.length === 0 ? <div className="grid min-h-[180px] place-items-center rounded-2xl border border-dashed border-[#171717]/15 text-center"><div><KeyRound className="mx-auto h-7 w-7 opacity-25" /><p className="mt-3 text-sm font-semibold">Nenhuma licença encontrada</p><p className="mt-1 text-xs text-[#171717]/45">A licença do fundador aparecerá aqui quando cadastrada.</p></div></div> : <table className="w-full min-w-[720px] text-left"><thead><tr className="border-b border-[#171717]/10 text-[10px] uppercase tracking-[.14em] text-[#171717]/40"><th className="pb-3 font-semibold">Cliente</th><th className="pb-3 font-semibold">Plano</th><th className="pb-3 font-semibold">Licença</th><th className="pb-3 font-semibold">Status</th><th className="pb-3 text-right font-semibold">Criada em</th><th /></tr></thead><tbody>{filtered.map((item) => <tr key={item.key} className="border-b border-[#171717]/7 last:border-0"><td className="py-4"><div><div className="text-xs font-semibold">{item.name}</div><div className="mt-0.5 text-[10px] text-[#171717]/40">{item.email}</div></div></td><td className="py-4 text-xs text-[#171717]/65">{item.plan}</td><td className="py-4"><button onClick={() => { navigator.clipboard?.writeText(item.key); toast.success("Chave copiada."); }} className="flex items-center gap-2 font-mono text-[10px] text-[#171717]/60 hover:text-[#171717]">{item.key}<Copy className="h-3 w-3" /></button></td><td className="py-4"><StatusPill status={item.status} /></td><td className="py-4 text-right text-[10px] text-[#171717]/40">{formatTime(item.createdAt)}</td><td className="py-4 text-right"><button onClick={() => toast.info(`Detalhes de ${item.name}`)}><MoreHorizontal className="h-4 w-4 opacity-45" /></button></td></tr>)}</tbody></table>}</div></section>
        <div className="mt-4 grid gap-4 md:grid-cols-3"><div className="rounded-[20px] bg-[#e11d2e] p-5"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-[#171717] text-[#e11d2e]"><Smartphone className="h-4 w-4" /></div><span className="text-xs font-semibold">Dispositivos conectados</span></div><div className="mt-6 flex items-end justify-between"><span className="text-3xl font-semibold tracking-[-.07em]">{summary?.devices || 0}</span><span className="text-[10px] font-bold opacity-60">Dados reais</span></div></div><div className="rounded-[20px] border border-[#171717]/10 bg-white/45 p-5"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-[#d9f5b2]"><KeyRound className="h-4 w-4" /></div><span className="text-xs font-semibold">Licença do fundador</span></div><div className="mt-6 flex items-end justify-between"><span className="text-3xl font-semibold tracking-[-.07em]">{summary?.distribution.active === 1 ? "Ativa" : "—"}</span><span className="text-[10px] font-bold text-[#4e8a26]">{summary?.distribution.active === 1 ? "1 licença" : "Nenhuma"}</span></div></div><div className="rounded-[20px] border border-[#171717]/10 bg-white/45 p-5"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-[#d7e7fa]"><CircleHelp className="h-4 w-4" /></div><span className="text-xs font-semibold">Registros de pagamento</span></div><div className="mt-6 flex items-end justify-between"><span className="text-3xl font-semibold tracking-[-.07em]">{summary?.totals?.[0]?.change === "Sem assinaturas" ? 0 : "—"}</span><span className="text-[10px] font-bold text-[#4a79a4]">Stripe</span></div></div></div>
      </div></main></div>;
}
